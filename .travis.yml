language: ruby

rvm:
  - 2.3.3

before_script:
  - gem build ox.gemspec
  - gem install ox-*
  - cd ext/ox
  - ruby extconf.rb && make clean all
  - cd ../..

script:
  - ./test/tests.rb
  - ./test/sax/sax_test.rb
  - ./test/sax/smart_test.rb
